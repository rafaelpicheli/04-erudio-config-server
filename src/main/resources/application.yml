server:
  port: 8888
spring:
  application:
    name: 04-erudio-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rafaelpicheli/erudio-config-server
          #username: username
          #password: password
          default-label: main
          search-paths:
          - 'greeting-service*'